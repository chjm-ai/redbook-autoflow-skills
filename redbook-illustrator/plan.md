# 备忘录模板与长图分页实现计划

## 目标
- 新增 `memo` 样式，正文仿苹果备忘录。
- `memo` 不生成封面，直接输出正文分页图。
- 分页改为先渲染长图，再按 `1440px` 高度与 `96px` 重叠裁切。

## 实现步骤
1. 添加最小回归测试，覆盖 `memo` 样式注册、无封面行为、分页区间与切点微调。
2. 改造 `scripts/render_xhs_v2.js`，为 `memo` 增加独立长图渲染与裁切分支。
3. 新增 memo 模板与样式，去掉黑边和厚重阴影。
4. 更新 `SKILL.md`，补充本地 `README.md`，说明 `memo` 行为和用法。
5. 运行测试与渲染命令验证输出。
